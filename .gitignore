# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# Environment / logs
.env
*.log
vision_debug.log

# Generated outputs
output.json
output_fast.json
output_full.json
output_fast_annotated.json

# Next.js
frontend/node_modules/
frontend/.next/
frontend/out/

# Editor / OS
.vscode/
.idea/
.DS_Store

# Model artifacts
backend/models/
backend/**/hf_cache/
backend/**/*.pth
backend/**/*.pt
backend/**/*.bin
backend/**/*.ckpt
backend/**/*.safetensors

# Misc
dist/
build/
# backend virtualenv (recreate with backend/app/scripts/start_backend.sh header)
backend/.venv/
frontend/node_modules/

# frontend skills copied in from ai_projects/CLAUDE/promzy — local only, not committed
.claude/skills/

# personal profile export and thesis source material — local only, never committed
Profile.pdf
thesis/

# source copy of the public portrait, repo root only
/Promise-Adiole.png

# source copy of the RViz still, repo root only; frontend/public/rviz_romr.png
# is the served one and MUST be committed for the landing page to render
/rviz_romr.png

# Generated caches and scratch recordings
/backend/app/data/rag_cache/
/TMP_RECORDING.mp4

# Vercel link metadata
.vercel/


# tf2_tools view_frames output
frames_*.gv
frames_*.pdf

# Stray colcon trees. The real workspace is ~/turtlebot3_ws; see scripts/build.sh.
install/
log/

# Local vector index and runtime trace log: both are regenerated, not source.
/backend/app/data/chroma/
/backend/app/data/traces/

# LibreOffice lock files
.~lock.*#

# Thesis PDF and application slides: same class as thesis/ above, local only
/ROMR_Thesis.pdf
/ROMR_Portsmouth_Slides.pptx

# Root-level figure sources. The served/served-from copies live under
# docs/images/ and results/; these are the working originals.
/ROMR.jpeg
/ROMR1.jpeg
/gazebo_world.png
/robot_in_gazebo.png
/lagra_system_architecture.png
Thesis format.jpeg

# Screen recordings. Only the finished demo is versioned; the unedited master
# and superseded takes stay local, because video never leaves git history once
# committed and these are regenerable with scripts/record_screen.sh.
recordings/

# Unpublished co-authored manuscript. Lives with the project it reports on,
# but is not distributed: the co-authors have not agreed to publication.
paper/
